services:
  player-alert-bot:
    build: .
    container_name: player-alert-bot
    restart: unless-stopped
    user: "${PUID:-0}:${PGID:-0}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
